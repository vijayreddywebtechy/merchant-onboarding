import DirectorDetails from "@/components/onboarding/DirectorDetails";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <DirectorDetails />
    </>
  );
};

export default page;
